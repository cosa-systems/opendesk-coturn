coturn deployed and running:

- Listening: {{ .Values.coturn.externalIP }}:{{ .Values.coturn.listeningPort }} (UDP & TCP)
{{- if and .Values.coturn.tls.enabled }}
- Listening (TLS): {{ .Values.coturn.externalIP }}:{{ .Values.coturn.tls.listeningPort }} (UDP & TCP)
{{- end }}
minimum communication port: {{ .Values.coturn.minPort }}/UDP
maximum communication port: {{ .Values.coturn.maxPort }}/UDP
